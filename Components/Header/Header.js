import React from 'react'
import { View } from 'react-native'

function Header() {
    return (
        <View style = {styles.header}>
            <Text>Checkout our latest designs</Text>

        </View>
    );
};

const styles = StyleSheet.create({
    header: {

    },
});

export default Header
